import { jsx as _jsx } from "react/jsx-runtime";
import { useState } from 'react';
import { EventRailsProvider } from '../../context/EventRailsProvider';
import { useMediaListener } from '../../hooks';
import { blend, toTwoDigits } from '../../utils';
import { PROGRESS_BAR_DIVIDER } from '../../utils/constants';
import { EventRail } from './components/EventRail';
import { ProgressBarStyled } from './components/ProgressBarStyled';
/**
 * A MUI Slider configured for displaying currentTime/duration values from `MediaStore`
 * uses `EmitterListeners` for displaying data
 * @category React Component
 * @category UI Controls
 */
export const EventBasedProgressBar = ({ mediaListener, highlights = [], getHighlightColorBlended = blend, setCurrentTime, ...props }) => {
    const [value, setValue] = useState(0);
    const [duration, setDuration] = useState(0);
    const onCurrentTimeUpdate = (e, newValue, _activeThumb) => {
        e.preventDefault();
        if (Array.isArray(newValue)) {
            return;
        }
        // Get new time according to played time from the total media duration
        const seekTime = (newValue / PROGRESS_BAR_DIVIDER) * duration;
        setCurrentTime === null || setCurrentTime === void 0 ? void 0 : setCurrentTime(seekTime);
    };
    useMediaListener('durationchange', e => setDuration(e.duration), mediaListener);
    useMediaListener('timeupdate', ({ duration, seconds }) => {
        if (duration && seconds) {
            // Keep progress value as 0.01, 0.02, 0.03,
            // doing them as 0.0000001, 0.00121121 - you wont feel difference
            return setValue(toTwoDigits((seconds / duration) * PROGRESS_BAR_DIVIDER));
        }
        return setValue(0);
    }, mediaListener);
    return (_jsx(EventRailsProvider, { highlights: highlights, duration: duration, getHighlightColorBlended: getHighlightColorBlended, children: _jsx(ProgressBarStyled, { min: 0, max: PROGRESS_BAR_DIVIDER, onChange: onCurrentTimeUpdate, value: value, components: { Rail: EventRail }, ...props }) }));
};
//# sourceMappingURL=EventBasedProgressBar.js.map