import { jsx as _jsx } from "react/jsx-runtime";
import { useEventRailsContext } from '../../../hooks/use-event-rails-context';
import { getPercentFromDuration } from '../../../utils/highlights';
import { RailStyled } from './RailStyled';
import { useRailStyles } from './useRailStyles';
const BLEND_CONFIG = { intensifyAll: true };
export const EventRail = () => {
    const { duration, getHighlightColorBlended, highlights } = useEventRailsContext();
    const { sliderRail } = useRailStyles().classes;
    return (_jsx("div", { className: sliderRail, children: highlights === null || highlights === void 0 ? void 0 : highlights.map(({ id, colors, start, end }) => {
            const highlightEdgesColor = getHighlightColorBlended === null || getHighlightColorBlended === void 0 ? void 0 : getHighlightColorBlended(colors, BLEND_CONFIG);
            return (_jsx(RailStyled, { startPoint: getPercentFromDuration(start, duration), width: getPercentFromDuration(end - start, duration), color: getHighlightColorBlended === null || getHighlightColorBlended === void 0 ? void 0 : getHighlightColorBlended(colors), startColorSegment: highlightEdgesColor, endColorSegment: highlightEdgesColor }, id));
        }) }));
};
//# sourceMappingURL=EventRail.js.map