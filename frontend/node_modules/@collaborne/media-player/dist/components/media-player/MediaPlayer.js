import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { memo } from 'react';
import { CorePlayer } from '../core-player/CorePlayer';
import { MediaPlayerControls } from './components/MediaPlayerControls';
import { useMediaPlayerStyles } from './useMediaPlayerStyles';
/**
 * Out of the box media player, with all functionality and UI included
 * @category React Component
 * @category Player
 */
export const MediaPlayer = memo(({ children, className, classes, collapse, onToggleCollapse, ...corePlayerProps }) => {
    var _a;
    const isCollapsed = Boolean(collapse);
    const { classes: mediaClasses, cx } = useMediaPlayerStyles();
    const collapsedClassName = cx({
        [(_a = classes === null || classes === void 0 ? void 0 : classes.collapsedClassName) !== null && _a !== void 0 ? _a : mediaClasses.collapsedContainer]: isCollapsed,
    });
    return (_jsxs(CorePlayer, { ...corePlayerProps, className: cx(className, collapsedClassName), isPipEnabled: false, children: [_jsx(MediaPlayerControls, { isCollapsed: isCollapsed, toggleCollapse: onToggleCollapse }), children] }));
});
//# sourceMappingURL=MediaPlayer.js.map