import { FC, ReactNode } from 'react';
import { CorePlayerProps } from '../core-player/CorePlayer';
export interface MediaPlayerProps extends Omit<CorePlayerProps, 'children' | 'isPipEnabled' | 'pipPortalClassName' | 'pipContainer'> {
    classes?: {
        collapsedClassName?: string;
    };
    children?: ReactNode;
    collapse?: boolean;
    onToggleCollapse?: VoidFunction;
}
/**
 * Out of the box media player, with all functionality and UI included
 * @category React Component
 * @category Player
 */
export declare const MediaPlayer: FC<MediaPlayerProps>;
