import { jsx as _jsx } from "react/jsx-runtime";
import Grid from '@mui/material/Grid';
import { memo } from 'react';
import { useIsAudio } from '../../../hooks';
import { BOTTOM_CONTROL_BUTTONS } from '../../../utils';
import { useBottomControlButtonsHook } from '../../bottom-control-buttons/useBottomControlButtonsHook';
import { useBottomControlButtonsStyles } from '../../bottom-control-buttons/useBottomControlButtonsStyles';
/**
 * Wrapper that includes media player bottom controls buttons
 * @category React Component
 * @category UI Controls
 */
export const MediaPlayerControlButtons = memo(({ className, children, isCollapsed, 'data-testid': dataTestId = BOTTOM_CONTROL_BUTTONS, }) => {
    const isAudio = useIsAudio();
    const { classes, cx } = useBottomControlButtonsStyles();
    const { showControls } = useBottomControlButtonsHook();
    const hide = !isCollapsed && !showControls && !isAudio;
    if (hide) {
        return null;
    }
    return (_jsx(Grid, { container: true, className: cx(classes.wrapper, className), alignItems: "stretch", justifyContent: "flex-start", direction: "column", "data-testid": dataTestId, children: children }));
});
//# sourceMappingURL=MediaPlayerControlButtons.js.map