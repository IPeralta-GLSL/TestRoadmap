import { makeStyles } from 'tss-react/mui';
export const useMediaPosterStyles = makeStyles()((theme, { img, ...props }) => {
    var _a, _b;
    return ({
        mediaPoster: {
            background: theme.palette.common.black,
            backgroundImage: img ? `url('${img}')` : 'none',
            width: (_a = props.width) !== null && _a !== void 0 ? _a : theme.spacing(40),
            height: (_b = props.height) !== null && _b !== void 0 ? _b : theme.spacing(40),
            backgroundSize: 'cover',
            backgroundPosition: 'center',
            ...props,
        },
    });
});
//# sourceMappingURL=useMediaPosterStyles.js.map