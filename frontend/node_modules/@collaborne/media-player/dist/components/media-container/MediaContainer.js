import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import Grid from '@mui/material/GridLegacy';
import { memo, useEffect, useRef } from 'react';
import intl from 'react-intl-universal';
import screenfull from 'screenfull';
import { shallow } from 'zustand/shallow';
import { useMediaStore } from '../../context/MediaProvider';
import { useIsAudio } from '../../hooks';
import { MEDIA_CONTAINER } from '../../utils';
import { DraggablePopover, } from '../draggable-popover/DraggablePopover';
import { MediaPoster } from '../media-poster/MediaPoster';
import { Player } from '../player/Player';
import { useIsPlayerReadyHook } from './useIsPlayerReadyHook';
import { useFilePlayerStyles, useMediaContainerStyles, } from './useMediaContainerStyles';
import { useMouseActivityHook } from './useMouseActivityHook';
import { UsePipHook } from './UsePipHook';
import { useReactPlayerProps } from './useReactPlayerProps';
/** A React Component that consumes MediaContext's API and adds UI for the player and media controls
 * @category React Component
 * @category UI Controls
 */
export const MediaContainer = memo(({ className, url, children, xAxisDistance, yAxisDistance, audioPlaceholder, reactPlayerClassName, pipContainer, pipPortalClassName, }) => {
    const isAudio = useIsAudio();
    // ref for the PIP area(pip will appear there)
    const pipAreaRef = useRef(null);
    const [mediaContainerRef, isPip, isFullscreen, isPipEnabled, setIsFullscreen,] = useMediaStore(state => [
        state.mediaContainerRef,
        state.isPip,
        state.isFullscreen,
        state.isPipEnabled,
        state.setIsFullscreen,
    ], shallow);
    const { classes: { wrapper, pipText, reactPlayer, pipArea }, cx, } = useMediaContainerStyles({
        isAudio,
    });
    const { wrapper: playerWrapper } = useFilePlayerStyles({ isAudio }).classes;
    const { isPlayerReady } = useIsPlayerReadyHook({ url });
    const { onMouseEnter, onMouseLeave, onMouseMove } = useMouseActivityHook();
    const { reactPlayerProps } = useReactPlayerProps();
    const reactClassNames = cx(reactPlayer, playerWrapper, reactPlayerClassName);
    const playerProps = {
        url,
        className: reactClassNames,
        isFullscreen,
        reactPlayerProps,
    };
    useEffect(() => {
        if (screenfull.isEnabled) {
            screenfull.on('change', () => {
                setIsFullscreen(screenfull.isFullscreen);
            });
        }
    }, [mediaContainerRef, setIsFullscreen]);
    const renderPlayer = () => isPipEnabled ? (_jsxs(_Fragment, { children: [_jsx(DraggablePopover, { audioPlaceholder: audioPlaceholder, xAxisDistance: xAxisDistance, yAxisDistance: yAxisDistance, pipDraggableAreaRef: pipContainer || pipAreaRef, pipPortalClassName: pipPortalClassName, children: _jsx(Player, { ...playerProps }) }), isPip && !isAudio && (_jsx(MediaPoster, { width: "100%", height: "100%", children: _jsx("div", { className: pipText, children: intl.get('media.playing_pip') }) }))] })) : (_jsx(Player, { ...playerProps }));
    // TODO: Add a UI/UX decision when player is not ready
    if (!isPlayerReady || !url) {
        return null;
    }
    return (_jsxs(_Fragment, { children: [isPipEnabled && _jsx(UsePipHook, { isPlayerReady: isPlayerReady }), !pipContainer && (_jsx(Grid, { ref: pipAreaRef, className: cx(pipArea, pipPortalClassName) })), _jsxs("div", { ref: mediaContainerRef, className: cx(wrapper, className), onMouseEnter: onMouseEnter, onMouseLeave: onMouseLeave, onMouseMove: onMouseMove, "data-testid": MEDIA_CONTAINER, children: [renderPlayer(), children] })] }));
});
//# sourceMappingURL=MediaContainer.js.map