import { FC } from 'react';
import { CorePlayerProps } from '../core-player/CorePlayer';
import { DraggablePopoverProps } from '../draggable-popover/DraggablePopover';
export interface MediaContainerProps extends Pick<CorePlayerProps, 'children' | 'url' | 'className' | 'reactPlayerClassName' | 'pipContainer'>, Pick<DraggablePopoverProps, 'audioPlaceholder' | 'xAxisDistance' | 'yAxisDistance' | 'pipPortalClassName'> {
    url: string;
}
/** A React Component that consumes MediaContext's API and adds UI for the player and media controls
 * @category React Component
 * @category UI Controls
 */
export declare const MediaContainer: FC<MediaContainerProps>;
