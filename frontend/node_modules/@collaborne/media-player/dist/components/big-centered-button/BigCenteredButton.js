import { jsx as _jsx } from "react/jsx-runtime";
import { IconButton } from '@mui/material';
import { useBigCenteredButtonStyles } from './useBigCenteredButtonStyles';
/**
 * A component that adds a big centered icon button with corresponding styles and backdrop
 * @category React Component
 */
export const BigCenteredButton = ({ onClick, classNames, iconButtonProps, Icon, }) => {
    const { classes, cx } = useBigCenteredButtonStyles();
    return (_jsx("div", { className: cx(classes.controlsWrapper, classNames), onClick: onClick, children: _jsx(IconButton, { className: classes.button, ...iconButtonProps, children: _jsx(Icon, { className: classes.svgStyle }) }) }));
};
//# sourceMappingURL=BigCenteredButton.js.map