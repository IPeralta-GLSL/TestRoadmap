import { MediaType } from '../../../types/media-type';
interface UseCorePlayerHookProps {
    url?: string;
    initialMediaType?: string;
}
interface UseCorePlayerHook {
    mediaType: MediaType;
}
export declare const useCorePlayerHook: ({ url, initialMediaType, }: UseCorePlayerHookProps) => UseCorePlayerHook;
export {};
