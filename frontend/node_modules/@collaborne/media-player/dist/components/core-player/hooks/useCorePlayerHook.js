import { useMemo } from 'react';
import { getMediaType } from './media-type';
export const useCorePlayerHook = ({ url, initialMediaType, }) => {
    const mediaType = useMemo(() => getMediaType({ url, initialMediaType }), [url, initialMediaType]);
    return { mediaType };
};
//# sourceMappingURL=useCorePlayerHook.js.map