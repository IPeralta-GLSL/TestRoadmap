import { MediaType } from '../../../types/media-type';
interface UseCorePlayerHookProps {
    url?: string;
    initialMediaType?: string;
}
export declare function getMediaType({ url, initialMediaType, }: UseCorePlayerHookProps): MediaType;
export {};
