import { isAudio, isUrlSupported } from '../../../utils/is-url-supported';
export function getMediaType({ url, initialMediaType, }) {
    if (!url) {
        return 'unknown';
    }
    // If mediaType was initialized external => we use it
    if (initialMediaType) {
        if (initialMediaType.startsWith('audio/')) {
            return 'audio';
        }
        else if (initialMediaType.startsWith('video/')) {
            return 'video';
        }
        else {
            return 'unknown';
        }
    }
    // Otherwise we define it from URL
    if (isUrlSupported(url)) {
        if (isAudio(url)) {
            return 'audio';
        }
        return 'video';
    }
    return 'unknown';
}
//# sourceMappingURL=media-type.js.map