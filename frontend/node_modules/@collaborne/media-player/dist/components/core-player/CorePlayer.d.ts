import { Theme } from '@mui/material/styles';
import { FC, ReactNode, RefObject } from 'react';
import { MediaProviderProps } from '../../context/MediaProvider';
import { DraggablePopoverProps } from '../draggable-popover/DraggablePopover';
export interface CorePlayerProps extends Partial<Pick<MediaProviderProps, 'initialState' | 'getHighlightColorBlended' | 'onStoreUpdate' | 'highlights' | 'alarms' | 'isPipEnabled'>>, Pick<DraggablePopoverProps, 'audioPlaceholder' | 'xAxisDistance' | 'yAxisDistance' | 'pipPortalClassName'> {
    /** The url of the media file to be played */
    url: string;
    /** CSS class name applied to component  */
    className?: string;
    /** A MUI theme to control the stylization of the player . */
    theme?: Theme;
    children: ReactNode;
    /** Url mime type */
    mediaType?: string;
    /** Builds UI for the PIP Player */
    PIPControls?: FC;
    /** <video /> tags wrapper className */
    reactPlayerClassName?: string;
    /** Container where PIP player will be mounted By default PIP player is added as a child of document.body */
    pipContainer?: RefObject<HTMLDivElement | null>;
}
/**
 * The core of the player. With all media logics and functionality but without any UI Controls .
 * @category React Component
 * @category Player
 */
export declare const CorePlayer: FC<CorePlayerProps>;
