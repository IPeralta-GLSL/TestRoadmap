import { useCallback, useEffect, useLayoutEffect, useRef } from 'react';
import useUnmount from 'react-use/lib/useUnmount';
import { shallow } from 'zustand/shallow';
import { useMediaStore } from '../../context';
export const usePlayerHook = ({ url }) => {
    const hasAutoplayedRef = useRef(false);
    const [reactPlayerRef, mediaContainerRef, initialState, isPlaying, emitter, onPause, setCurrentTime, isPip, onPlay,] = useMediaStore(state => [
        state.reactPlayerRef,
        state.mediaContainerRef,
        state.initialState,
        state.isPlaying,
        state.emitter,
        state.pause,
        state.setCurrentTime,
        state.isPip,
        state.play,
    ], shallow);
    const onReadyToPlay = useCallback(() => {
        var _a;
        const mediaEl = (_a = reactPlayerRef === null || reactPlayerRef === void 0 ? void 0 : reactPlayerRef.current) === null || _a === void 0 ? void 0 : _a.getInternalPlayer();
        emitter.off('seeked', onReadyToPlay);
        mediaEl === null || mediaEl === void 0 ? void 0 : mediaEl.play().then(() => emitter.emit('autoplayStart')).catch((error) => {
            console.info('Player failed to autoplay', error);
            onPause();
        });
    }, [onPause, emitter, reactPlayerRef]);
    // Play is a async operation. so when the player is ready to autoplay media,
    // then we must be sure that first of all we solve setCurrentTime and after that the play method.
    // https://developer.mozilla.org/en-US/docs/Web/API/HTMLMediaElement/play
    const onReadyToSeek = useCallback(() => {
        emitter.on('seeked', onReadyToPlay);
        emitter.off('ready', onReadyToSeek);
        window.setTimeout(() => {
            setCurrentTime(0);
        }, 0);
    }, [emitter, onReadyToPlay, setCurrentTime]);
    useLayoutEffect(() => {
        var _a, _b, _c;
        if (!hasAutoplayedRef.current &&
            (reactPlayerRef === null || reactPlayerRef === void 0 ? void 0 : reactPlayerRef.current) &&
            initialState.autoPlay) {
            const el = (_a = reactPlayerRef === null || reactPlayerRef === void 0 ? void 0 : reactPlayerRef.current) === null || _a === void 0 ? void 0 : _a.getInternalPlayer();
            if (el && el.parentElement) {
                (_b = el.parentElement) === null || _b === void 0 ? void 0 : _b.focus();
            }
            const mediaEl = (_c = reactPlayerRef.current) === null || _c === void 0 ? void 0 : _c.getInternalPlayer();
            if (!mediaEl)
                return;
            emitter.on('ready', onReadyToSeek);
        }
        hasAutoplayedRef.current = true;
    }, [emitter, initialState.autoPlay, onReadyToSeek, reactPlayerRef]);
    const hasAutoFocusedRef = useRef(false);
    useLayoutEffect(() => {
        var _a;
        if (!url || hasAutoFocusedRef.current) {
            return;
        }
        const mediaContainerElement = (_a = reactPlayerRef === null || reactPlayerRef === void 0 ? void 0 : reactPlayerRef.current) === null || _a === void 0 ? void 0 : _a.wrapper;
        if (!mediaContainerElement) {
            throw new Error('mediaContainerElement can not be null after componentDidMount.');
        }
        const timeoutId = setTimeout(() => {
            mediaContainerElement.focus();
            hasAutoFocusedRef.current = true;
        }, 100);
        return () => clearTimeout(timeoutId);
    }, [url, reactPlayerRef]);
    useUnmount(() => {
        var _a;
        // Bug: media is stuck browser memory, so even after dismount the OS play/pause controls work
        // Clear src attribute so it's removed.
        const mediaEl = (_a = mediaContainerRef === null || mediaContainerRef === void 0 ? void 0 : mediaContainerRef.current) === null || _a === void 0 ? void 0 : _a.querySelector('media');
        if (mediaEl) {
            mediaEl.setAttribute('src', '');
        }
    });
    const togglePlay = useCallback(() => {
        // PIP mode disables clicking on screen to toggle isPlaying
        if (isPip) {
            return;
        }
        if (!isPlaying) {
            return onPlay();
        }
        return onPause();
    }, [isPip, isPlaying, onPause, onPlay]);
    // Add stop/pause events on clicking to media-player
    useEffect(() => {
        var _a;
        const mediaContainerElement = (_a = reactPlayerRef === null || reactPlayerRef === void 0 ? void 0 : reactPlayerRef.current) === null || _a === void 0 ? void 0 : _a.wrapper;
        if (mediaContainerElement == null) {
            return console.error('mediaContainerElement can not be null after componentDidMount.');
        }
        mediaContainerElement.addEventListener('click', togglePlay);
        return () => {
            mediaContainerElement.removeEventListener('click', togglePlay);
        };
    }, [reactPlayerRef, togglePlay]);
    return {};
};
//# sourceMappingURL=usePlayerHook.js.map