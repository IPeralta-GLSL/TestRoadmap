import { jsx as _jsx } from "react/jsx-runtime";
import { memo } from 'react';
import ReactPlayer from 'react-player';
import { PROGRESS_INTERVAL, REACT_PLAYER } from '../../utils';
import { usePlayerHook } from './usePlayerHook';
/**
 * Serves for collecting all props from `MediaStore` and passing them to ReactPlayer
 * @category React Component
 */
export const Player = memo(({ url, className, reactPlayerProps }) => {
    usePlayerHook({ url });
    return (_jsx(ReactPlayer, { url: url, progressInterval: PROGRESS_INTERVAL, width: "unset", height: "100%", className: className, "data-testid": REACT_PLAYER, config: {
            file: {
                attributes: {
                    crossOrigin: 'anonymous',
                    preload: 'false',
                    autoPlay: reactPlayerProps.autoPlay,
                },
            },
        }, ...reactPlayerProps }));
});
//# sourceMappingURL=Player.js.map