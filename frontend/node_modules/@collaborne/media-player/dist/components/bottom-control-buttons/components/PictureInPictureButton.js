import { jsx as _jsx } from "react/jsx-runtime";
import { IconButton } from '@mui/material';
import { PiPictureInPicture } from 'react-icons/pi';
import { shallow } from 'zustand/shallow';
import { useMediaStore } from '../../../context';
import { useOnHoveredControlElement } from '../../../hooks/use-on-hovered-element';
import { PIP_BUTTON } from '../../../utils';
/**
 * @category React Component
 * @category UI Controls
 */
export const PictureInPictureButton = ({ Icon = PiPictureInPicture, svgIconProps, 'data-testid': dataTestId = PIP_BUTTON, ...props }) => {
    const { onMouseEnter, onMouseLeave } = useOnHoveredControlElement();
    const [isPip, exitPip, requestPip, setHasPipTriggeredByClick] = useMediaStore(state => [
        state.isPip,
        state.exitPip,
        state.requestPip,
        state.setHasPipTriggeredByClick,
    ], shallow);
    const togglePip = () => {
        if (isPip) {
            setHasPipTriggeredByClick(true);
            return exitPip();
        }
        return requestPip();
    };
    return (_jsx(IconButton, { onMouseEnter: onMouseEnter, onMouseLeave: onMouseLeave, size: "medium", onClick: togglePip, "data-testid": dataTestId, ...props, children: _jsx(Icon, { fontSize: "medium", ...svgIconProps }) }));
};
//# sourceMappingURL=PictureInPictureButton.js.map