import { IconButtonProps, SvgIconProps } from '@mui/material';
import { ComponentType, FC } from 'react';
interface PlayPauseReplayProps extends IconButtonProps {
    svgClassName?: string;
    Icons?: Record<'Play' | 'Replay' | 'Pause', ComponentType<SvgIconProps>>;
    svgIconSize?: SvgIconProps['fontSize'];
    skipSeconds?: number;
    'data-testid'?: string;
}
/**
 * @category React Component
 * @category UI Controls
 */
export declare const PlayPauseReplay: FC<PlayPauseReplayProps>;
export {};
