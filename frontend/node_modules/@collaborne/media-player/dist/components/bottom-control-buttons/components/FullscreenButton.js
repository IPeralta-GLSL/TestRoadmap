import { jsx as _jsx } from "react/jsx-runtime";
import { IconButton } from '@mui/material';
import { PiCornersIn, PiCornersOut } from 'react-icons/pi';
import { shallow } from 'zustand/shallow';
import { useMediaStore } from '../../../context';
import { useIsAudio } from '../../../hooks/use-is-audio';
import { useOnHoveredControlElement } from '../../../hooks/use-on-hovered-element';
import { FULLSCREEN_BUTTON } from '../../../utils';
/**
 * @category React Component
 * @category UI Controls
 */
export const FullscreenButton = ({ Icons = {
    FullscreenEnter: PiCornersOut,
    FullscreenExit: PiCornersIn,
}, svgIconProps, 'data-test-id': dataTestId = FULLSCREEN_BUTTON, ...props }) => {
    const [isFullscreen, requestFullscreen, exitFullscreen] = useMediaStore(state => [
        state.isFullscreen,
        state.requestFullscreen,
        state.exitFullscreen,
    ], shallow);
    const { onMouseEnter, onMouseLeave } = useOnHoveredControlElement();
    const isAudio = useIsAudio();
    const onFullscreen = isFullscreen ? exitFullscreen : requestFullscreen;
    if (isAudio) {
        return null;
    }
    return (_jsx(IconButton, { onMouseEnter: onMouseEnter, onMouseLeave: onMouseLeave, size: "medium", onClick: onFullscreen, "data-testid": dataTestId, ...props, children: !isFullscreen ? (_jsx(Icons.FullscreenEnter, { fontSize: "medium", ...svgIconProps })) : (_jsx(Icons.FullscreenExit, { fontSize: "medium", ...svgIconProps })) }));
};
//# sourceMappingURL=FullscreenButton.js.map