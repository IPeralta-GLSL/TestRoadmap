import { jsxs as _jsxs } from "react/jsx-runtime";
import { Typography } from '@mui/material';
import { useMediaStore } from '../../../context';
import { toTimestamp } from '../../../utils/time';
import { useTimeDisplayStyles } from '../hooks/useTimeDisplayStyles';
const SECONDS_MULTIPLIER = 1000;
/**
 * @category React Component
 * @category UI Controls
 */
export const TimeDisplay = ({ secondsMultiplier = SECONDS_MULTIPLIER, className, ...props }) => {
    const duration = useMediaStore(state => state.duration);
    const currentTime = useMediaStore(state => state.currentTime);
    const { classes: { timeStampText }, cx, } = useTimeDisplayStyles();
    return (_jsxs(Typography, { variant: "body2", className: cx(timeStampText, className), color: "inherit", ...props, children: [toTimestamp(currentTime * secondsMultiplier), " /", ' ', toTimestamp(duration * secondsMultiplier)] }));
};
//# sourceMappingURL=TimeDisplay.js.map