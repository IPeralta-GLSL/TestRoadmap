import { jsx as _jsx } from "react/jsx-runtime";
import { IconButton } from '@mui/material';
import { shallow } from 'zustand/shallow';
import { useMediaStore } from '../../../context';
import { useOnHoveredControlElement } from '../../../hooks/use-on-hovered-element';
import { VOLUME_MULTIPLIER } from '../../../utils/constants';
import { VolumeIcon } from './VolumeIcon';
/**
 * @category React Component
 * @category UI Controls
 */
export const VolumeButton = ({ Icon = VolumeIcon, svgIconProps, volumeMultiplier = VOLUME_MULTIPLIER, ...props }) => {
    const { onMouseEnter, onMouseLeave } = useOnHoveredControlElement();
    const [volume, mute, unmute, isMuted] = useMediaStore(state => [
        state.volume * volumeMultiplier,
        state.mute,
        state.unmute,
        state.isMuted,
    ], shallow);
    const onToggleMute = () => {
        return isMuted ? unmute() : mute();
    };
    return (_jsx(IconButton, { onMouseEnter: onMouseEnter, onMouseLeave: onMouseLeave, size: "medium", onClick: onToggleMute, "data-testid": "icon-volume", ...props, children: _jsx(Icon, { fontSize: "medium", volume: isMuted ? 0 : volume, ...svgIconProps }) }));
};
//# sourceMappingURL=VolumeButton.js.map