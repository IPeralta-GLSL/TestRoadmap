import { jsx as _jsx } from "react/jsx-runtime";
import { PiSpeakerSimpleHigh, PiSpeakerSimpleLow, PiSpeakerSimpleX, } from 'react-icons/pi';
import { MIN_VOLUME } from '../../../utils/constants';
/**
 * @category React Component
 * @category UI Controls
 */
export const VolumeIcon = ({ volume, Icons = {
    VolumeUp: PiSpeakerSimpleHigh,
    VolumeDown: PiSpeakerSimpleLow,
    VolumeMuted: PiSpeakerSimpleX,
}, minVolume = MIN_VOLUME, ...props }) => {
    if (volume === 0) {
        return _jsx(Icons.VolumeMuted, { ...props });
    }
    if (volume >= minVolume) {
        return _jsx(Icons.VolumeUp, { ...props });
    }
    return _jsx(Icons.VolumeDown, { ...props });
};
//# sourceMappingURL=VolumeIcon.js.map