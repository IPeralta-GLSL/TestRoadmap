import { jsx as _jsx } from "react/jsx-runtime";
import { useIsAudio } from '../../hooks';
import { useBottomControlsStyles } from './useBottomControlsStyles';
/**
 * Wrapper that includes bottom controls
 * @category React Component
 * @category UI Controls
 */
export const BottomControls = ({ children, className, }) => {
    const isAudio = useIsAudio();
    const { classes: { bottomControls }, cx, } = useBottomControlsStyles({ isAudio });
    return _jsx("div", { className: cx(bottomControls, className), children: children });
};
//# sourceMappingURL=BottomControls.js.map