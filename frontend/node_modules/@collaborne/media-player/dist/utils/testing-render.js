import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { ThemeProvider } from '@mui/material/styles';
import { render as renderRTL } from '@testing-library/react';
import { CORE_PLAYER_INITIAL_STATE } from '../components/core-player/types';
import { MediaProvider, useMediaStore, } from '../context/MediaProvider';
import { createPlayerTheme } from '../theme';
import { blend } from './colors';
export * from '@testing-library/react';
export { default as userEvent } from '@testing-library/user-event';
const playerTheme = createPlayerTheme();
export const renderWithProviders = (Component, options) => renderRTL(_jsx(MediaProvider, { isPipEnabled: true, initialState: CORE_PLAYER_INITIAL_STATE, getHighlightColorBlended: blend, isAudio: false, mediaType: "video", children: _jsx(ThemeProvider, { theme: playerTheme, children: Component }) }), options);
/** Setup test for a component that is dependant of the `MediaProvider` context */
export function setupMediaProvider(Component, mediaProviderProps) {
    const returnVal = {};
    function NullComponent() {
        const mediaStore = useMediaStore();
        Object.assign(returnVal, mediaStore);
        return null;
    }
    function TestComponent() {
        return (_jsx(MediaProvider, { isPipEnabled: true, initialState: CORE_PLAYER_INITIAL_STATE, getHighlightColorBlended: blend, mediaType: "video", isAudio: false, ...mediaProviderProps, children: _jsxs(ThemeProvider, { theme: playerTheme, children: [Component, _jsx(NullComponent, {})] }) }));
    }
    return { ...renderRTL(_jsx(TestComponent, {})), mediaStore: returnVal };
}
//# sourceMappingURL=testing-render.js.map