import { MediaType } from '../types';
/**
 * Selector for `mediaType` from `MediaStore`
 * @category hooks
 * @category MediaStore
 */
export declare const useMediaType: () => {
    mediaType: MediaType;
};
