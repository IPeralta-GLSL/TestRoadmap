import { renderHook } from '@testing-library/react';
import mitt from 'mitt';
import { useMediaListener } from '../use-media-listener';
const emitter = mitt();
const eventName = 'play';
const emitApi = {
    addEventListener: emitter.on,
    removeEventListener: emitter.off,
};
const addEventListenerSpy = jest.spyOn(emitApi, 'addEventListener');
const removeEventListenerSpy = jest.spyOn(emitApi, 'removeEventListener');
describe('useMediaListener', () => {
    it(`mount/unmount on "${eventName}" event`, async () => {
        const handler = jest.fn();
        const { unmount } = renderHook(() => useMediaListener(eventName, handler, emitApi));
        expect(addEventListenerSpy).toHaveBeenCalledTimes(1);
        expect(addEventListenerSpy).toHaveBeenCalledWith(eventName, expect.any(Function));
        unmount();
        expect(removeEventListenerSpy).toHaveBeenCalledWith(eventName, expect.any(Function));
    });
});
//# sourceMappingURL=use-media-listener.spec.js.map