import { useCallback, useRef, useState } from 'react';
/**
 * Get the player store external, via assigning it to a `ref` for optimization
 * @category hooks
 * @category MediaStore
 */
export const usePlayerContext = () => {
    // Rerender when media context exists/ready
    const [, setIsReady] = useState(false);
    const mediaContextRef = useRef(undefined);
    const setMediaContext = useCallback((store) => {
        mediaContextRef.current = store;
        if (store) {
            setIsReady(true);
        }
    }, []);
    const mediaContext = mediaContextRef.current;
    return {
        setMediaContext,
        mediaContextRef,
        mediaContext,
    };
};
//# sourceMappingURL=use-player-context.js.map