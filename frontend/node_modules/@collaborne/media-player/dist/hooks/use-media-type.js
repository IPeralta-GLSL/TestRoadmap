import { useMediaStore } from '../context/MediaProvider';
/**
 * Selector for `mediaType` from `MediaStore`
 * @category hooks
 * @category MediaStore
 */
export const useMediaType = () => {
    const mediaType = useMediaStore(state => state.mediaType);
    return { mediaType };
};
//# sourceMappingURL=use-media-type.js.map